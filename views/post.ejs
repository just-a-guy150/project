<!DOCTYPE html>
<html lang="en">
<%- include('head.ejs') %>
<body>
        <%- include('header.ejs') %>
            <div class="wrapper">
                <div class="ad">

                    <div class="imgs">
                        <% product.image.forEach( (image)=> { %>
                            <img src="/uploads/<%= image %>" alt="">
                            <% }) %>
                    </div>
                    <h2>
                        <%= product.title %>
                    </h2>
                    <p>
                        <%= product.description %>
                    </p>

                </div>
                <%- include('postform.ejs') %>
                <%- include('commentlist.ejs', {comments: product.comments}) %>
                <% console.log(product); %>
            </div>
        <%- include('footer.ejs') %>
    </body>

</html>